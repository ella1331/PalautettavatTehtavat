function amstronginLuvut()
{
  var It;
  for(var x = 100; x < 1000; x++)
  {
    lt = x.toString().split("");
    if(math.pow(It[0], 3)+Math.pow(lt[1],3)+MAth.pow(lt[2],3) == x)
    {console.log(x);
    }
  }
}
amstronginLuvut();

function tahdet()
{
  var x,y,chr;
  for(x=1; x <=6; x++)
  {
    for (y=1; y < x; y++)

  {
    chr=chr+("*");
  }
  console.log(chr);
  chr="";
}
}
tahdet();
